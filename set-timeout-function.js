/*
Set Timeout Function:
* Set timeout function calls a function after the given millisecond.
*/

// After two seconds it will be run.
setTimeout(function(){
    console.log("It works after two seconds!");
}, 2000);